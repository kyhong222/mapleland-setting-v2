# ── 프론트엔드 (Vite, VITE_ 접두사만 번들에 노출됨) ──
# 이 앱에는 서버 코드가 없다. 아래 셋이 전부다.

# 클라우드 연동(디스코드 로그인 · 계정 동기화 · 문의하기)
#   설정 절차: docs/cloud-sync.md §7
# anon key는 번들에 노출되는 공개값이다. 격리는 RLS가 담당한다.
# 셋 다 비어 있으면 로그인 UI가 숨겨지고 기존 localStorage 전용으로 동작한다
# (문의하기는 로그인이 필요하므로 이때 이용할 수 없다).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# 서비스 식별자. Supabase 프로젝트를 *.mapleland.st가 공유하므로 서비스마다 달라야 한다.
VITE_APP_ID=item-sim
