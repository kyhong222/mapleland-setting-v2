# N방컷 · DPM 기획서

피격데미지 섹션과 별개로, **주는 데미지** 기반의 처치 성능을 다룬다.

- **비보스 몹**: 특정 공격스킬로 "정확히 N방에 처치할 확률"을 방컷별로 계산.
- **보스 몹**: HP가 매우 커 N방이 무의미 → 공속 기반 **DPM(분당 데미지)** 계산.

> 상태: 기획 확정 진행 중. `⛳ 필요 데이터` 표시는 사용자(메랜 raw data 검수) 제공 대기.

---

## 1. 목표 · 분기

| 대상 | 판정 | 출력 |
|---|---|---|
| `monster.isBoss !== true` | 비보스 | 방컷별 확률표 (정확히 N방) |
| `monster.isBoss === true` | 보스 | DPM (+ 참고: 처치 소요시간) |

---

## 2. 한 타(라인) 데미지 분포

### 2.1 기본 물리 데미지 (모션 배수 K 기준)

기존 `attackPower.calcPhysical`와 동일한 식. 무기배수 K를 모션에 따라 바꿔 대입한다.

```
MAX(K) = (주스탯 × K + 부스탯) × 총공격력 / 100
MIN(K) = (주스탯 × K × 0.9 × 숙련도 + 부스탯) × 총공격력 / 100
```

- 한 라인의 **원 데미지 ~ Uniform[MIN(K), MAX(K)]** (min~max 균등분포 가정)
- 마법 스킬은 `calcMagic` 사용(모션 무관, spellAtk 계수 = 스킬 `mad`).

### 2.2 모션 처리 (모션차 있는 무기)

스윙 = `constMax`, 찌르기 = `constMin`. **평균 배수로 뭉개지 않고 모션 확률을 분포에 그대로 반영**한다.

일반 스킬 한 타 = 2성분 혼합분포:

```
한타 = p_찌르기 × Uniform[MIN(constMin), MAX(constMin)]
     + p_베기   × Uniform[MIN(constMax), MAX(constMax)]
```

| 무기 | constMin(찌) / constMax(베) | p_찌르기 : p_베기 |
|---|---|---|
| 한손도끼 | 3.2 / 4.4 | 2 : 3 |
| 한손둔기 | 3.2 / 4.4 | 2 : 3 |
| 두손도끼 | 3.4 / 4.8 | 2 : 3 |
| 두손둔기 | 3.4 / 4.8 | 2 : 3 |
| 창 | 3.0 / 5.0 | 2 : 3 |
| 폴암 | 3.0 / 5.0 | 5 : 5 |

모션차 없는 무기(검·단검·활·완드·스태프 등 `constMin==constMax`)는 단일 균등분포.

### 2.3 스킬별 모션 규칙 (특수 케이스) — skill id 확정 ✅

**기본값 = 일반 2:3 혼합(2.2)**. 대부분 스킬이 평타 모션 기반이라 일반으로 처리하고, 아래 예외만 하드코딩.

| 스킬 | skill id | 라인 | 모션 |
|---|---|---|---|
| 브랜디쉬 | 1121008(히어로)·11111004(소마) | 2 (`attackCount`) | 찌1(constMin)+베1(constMax) |
| 스피어 버스터 | 1311001 | 2 | **2타 모두 찌르기**(constMin=3.0) |
| 폴암 버스터 | 1311002 | 2 | **2타 모두 찌르기**(constMin=3.0) |
| 드래곤 쓰레셔:창 | 1311003 | 1 | 항상 베기(constMax=5.0) |
| 드래곤 쓰레셔:폴암 | 1311004 | 1 | 항상 베기(constMax=5.0) |
| 드래곤 로어 | 1311006 | 1 | 4.0 고정 |
| 파이널 어택(전사 2차) | — | — | **미지원** |
| 그 외 일반 스킬 | — | raw | 일반 2:3 혼합(폴암 5:5) |

### 2.4 시전당 라인 수 — 확정 ✅

- **`attackCount` = 단일 대상에 들어가는 데미지 라인 수** (브랜디쉬·버스터 = 2). 미지정이면 1.
- `bulletCount` = 발사체/표창류 라인 수.
- `mobCount` = 타격 몬스터 수 → **N방컷은 단일 대상만 보므로 무관**(차지블로우 mobCount6도 대상 1마리엔 1라인).

시전 데미지 = 라인 분포들의 **합(컨볼루션)**. 라인마다 모션이 다르면(브랜디쉬 찌+베) 각각 합성.

---

## 3. 데미지 파이프라인 (라인 1개 기준)

`computeSkillDamage`(10단계) 재사용. 순서:

1. 기본 데미지 범위 `[min, max]` (2.1, 모션 K 반영)
2. 스킬 퍼뎀(`damage`) / 마법(`mad` 계수)
3. 속성 반응(약점 1.5 / 반감 0.5 / 무효 0)
4. 몬스터 방어 + 렙차 (`physicalVsMonster` / 마법식)
5. **데미지 증가 버프 배율** (4장) 곱
6. **위협 등 몬스터 방어 감소** (5장) 반영
7. **크리 기대배율** `f = 1 + 크리확률 × 크리추뎀/100` (7장)
8. 상한 199999 clamp, floor

결과: 라인 1개의 최종 분포 `~ Uniform[minF, maxF]` (모션 혼합이면 2성분).

---

## 4. 데미지 증가 버프 (직업별)

스킬 데미지 배율(파이프라인 5단계). 크리와 독립 곱연산. **파티버프(위협)와도 곱**.

버프별 skill id·원값(master)·모델링:

| 직업 | 버프 | skill id | 원값(master) | 모델링 |
|---|---|---|---|---|
| 히어로 | 콤보+어드밴스드콤보 | 1111002 · 1120003 | 아래 공식 | 최대카운터 = **×1.9**(콤보만 ×1.4) |
| 소울마스터 | 콤보+어드밴스드콤보 | 11111001 · 11110005 | 아래 공식 | 최대카운터 = **×1.8**(콤보만 ×1.33) |
| 팔라딘 | 차지(플/블/홀/썬)+어드밴스드차지 | 1211003~8·1221003~4·1220010 | dmg·z, 어드밴스드 dmg340 | **원소 부여 + 데미지 배율** — 정책 필요 ❓ |
| 다크나이트 | 버서크 | 1320006 | HP≤x%→dmg+% | **×1.48** (HP조건 무시, 배율만) ✅ |
| 아크메이지(FP/IL) | 엘앰프 | 2110001 · 2210001 | y140 → +40% | `amplifiedMagicDamageP`=y−100 (기구현 ✅) |
| 플레임위자드 | 엘앰프 | 12110001 | y118 → +18% | 동일 (기구현 ✅) |
| 플레임위자드 | 엘리멘탈 리셋 | 12101005 | 공격 무속성 변환 | **속성=none 처리**(배율 아님, §3-3) ❓ |
| 궁수 | 없음 | — | — | — |

### 콤보 어택 공식 (히어로 · 소울마스터) — 확정 ✅

용어(데이터 기준): `combo.damage`, `advCombo.damage`(레벨별), `x` = **최대 콤보 카운터**.
표기상 "퍼뎀" = `data.damage + 100`.

```
maxCounter = 어드밴스드콤보 학습 ? advCombo.x[advLv] : combo.x[comboLv]
카운터 c에서 콤보 뎀증(%):
  c ≤ 5:  combo.damage[comboLv] + (c-1) × (comboLv / 6)
  c ≥ 6:  advCombo.damage[advLv] + 20 + (c-5) × 4
데미지 배율 = 뎀증% / 100
```

- **N방컷/DPM은 최대 카운터 가정**(지속 유지). (원하면 카운터를 UI 입력으로)
- 계산 예:
  - 히어로 30/30 → 10c: 150+20+20 = 190% → **×1.9** / 콤보만 5c: 120+20 = 140% → **×1.4**
  - 소마 20/20 → 10c: 140+20+20 = 180% → **×1.8** / 콤보만 5c: 120+13.3 = 133% → **×1.33**

**패닉 / 코마** (콤보 카운터 전량 소모형): **N방컷·DPM 미제공, 데미지 범위만 표시.**
```
데미지 = 퍼뎀 × 콤보뎀증(위 배율) × 카운터뎀증
카운터뎀증: 1c=×1.0, 2c=×1.2, 3c=×1.54, 4c=×2.0, 5~10c=×2.5
예) 5c 패닉 350% × 1.4 × 2.5 = 1225% (10c: 350×1.9×2.5 = 1662%)
```

### 팔라딘 차지 — 속성반응 대체

차지 사용 시 공격이 **해당 속성**이 됨. §3-3의 고정 속성반응(약점1.5/반감0.5)을 **차지 스킬레벨 의존 배율로 대체**한다. (파이어=플레임, 아이스=블리자드)

**주차지 배율** (L = 차지 스킬레벨)
| 차지 | 약점(추뎀) | 반감 |
|---|---|---|
| 파이어·아이스·썬더 | `1.05 + L×0.015` | `0.95 − L×0.015` |
| 홀리 | `1.20 + L×0.015` | `0.80 − L×0.015` |

- 파/아/썬 마스터(L30) → 약점 1.50 / 반감 0.50. 홀리 마스터(L20) → 1.50 / 0.50.
- 몬스터 반응(elemAttr)에 따라: 약점→약점배율, 반감→반감배율, 무반응→×1.0, 무효→×0.

**썬더 스택(보조차지)** — 썬더차지는 단독 또는 다른 차지에 **중첩** 가능. 보조로 붙으면 계수가 줄어 **합연산**:
```
보조 썬더 기여 = (썬더약점배율 − 1) × 0.5 × 속성배수     // 예: (1.25 − 1) × 0.5 × 속성배수
```
조합: 플썬 · 블썬 · 홀썬 · 썬더단독.

❓ **확인 필요 (썬더 스택)**: 위 식의 `속성배수` 정의(= 몬스터의 썬더 반응? 주차지 배율?)와, 주차지 배율과 **어떻게 합산**되는지 — 구체 예시 하나(예: 플썬으로 "얼음약점+번개약점" 몹 타격 시 최종 속성배율) 부탁드립니다.

⛳ **남은 raw**: 썬더 스택 합산식 확정, 엘리멘탈 리셋 처리 확인, 중첩 규칙(곱연산).

---

## 5. 위협 (팔라딘, 개편) — 확정 ✅

skill id **1201006**, master 20. (출처: ms-skill-simulator `120.json`)

상세: *"MP N 소비, 확률 prop%, time1초간 적 공격력 −pad%, **데미지 +damage%**, time2초간 명중률 −acc%"*

- **N방컷/DPM 관련 효과 = "몬스터가 받는 데미지 +damage%"** → 데미지 배율 `× (1 + damage/100)` (4장 배율과 동일 취급).
- 나머지(적 공격력 −pad%, 명중률 −acc%)는 **피격/생존 영역** → 이 섹션 계산엔 미반영(피격 섹션 대상).

**데미지 증가% (레벨별)**: 1~4:1, 5~8:2, 9~11:3, 12~14:4, 15~16:5, 17~18:6, 19~20:**7**

| 레벨 | 데미지 증가 | 적 공격력 | 명중률 | 확률 |
|---|---|---|---|---|
| 20(마스터) | +7% | −20% | −20% | 50% |

**관리 방식 = 파티버프** (샤프아이즈처럼). 특수 파이프라인 케이스가 아니라 버프로 등록하고, 켜져 있으면(**적용 기준**) 아래 효과를 적용:
- **몬스터 받는 데미지 +damage%** → 이 섹션(N방컷/DPM) 데미지 배율 (신규 효과 필요, §10)
- 적 공격력 −pad% · 명중 −acc% → **피격 섹션**이 동일 버프에서 읽어 반영
- 확률(50%)은 반영 안 함(적용 가정). 필요 시 옵션.

> 참고: 블로킹(1120005/1220006)도 개편(21레벨↑ 블록% 상향, 마스터 200 표기)됐으나 **방어 스킬**이라 본 섹션과 무관. 버프 데이터 재동기화는 별도 처리.

---

## 6. 크리티컬 (샤프아이즈)

- 소스: 버프 합산 `criticalP`(확률), `criticalDamage`(추뎀).
- **기대배율 근사**: 라인별 베르누이 전개 대신 범위 전체에 `f = 1 + criticalP × criticalDamage/100` 곱.
- N방컷·DPM 모두 동일 근사 사용.

---

## 7. N방컷 확률 (비보스)

1. 라인 최종 분포(3장) → 시전 분포(2.4, 라인 합) 구성.
2. 분포를 정수 데미지 bin으로 이산화(예: 폭 = max 데미지에 비례한 스텝).
3. N타 합 분포 = 시전 분포를 N회 **컨볼루션**.
4. 누적분포 `CDF(N) = P(N타 합 ≥ 몬스터 HP)`.
5. **정확히 N방** `= CDF(N) − CDF(N−1)`.

출력: N = 1..K (K=10 등) 각 방컷 확률 %. (모션 혼합 + min~max 변동이 확률로 반영됨)

- 성능: bin 수 × N × 라인 수 컨볼루션. bin ~수백, N ~10 → 충분히 가벼움.

---

## 8. DPM (보스)

```
DPM = 기대데미지_시전 × 분당_공격횟수
기대데미지_시전 = Σ 라인 기대데미지 (모션 확률가중 · 크리 기대 · 버프 반영)
분당_공격횟수 = 공속표(12장) 조회
```

- 보스 방어/렙차/속성 동일 적용.
- 참고값: 처치 소요시간 ≈ 보스HP / (DPM/60).

### 분당 공격횟수 결정 (공속표)

스킬은 3부류:
1. **공속 무관** — 고정 분당횟수 (예: 로어 30, 쉐도우웹 74).
2. **무기 공속 단계 의존** — 단계 **2(최고속)~9(최저속)**별 분당횟수.
3. **마법사** — 무기 공속 무관, **매직부스터** 유무만 영향(노말/부스터 2값). ※예외: 불독 **익스플로전**만 무기공속 영향(물리처럼 처리).

**무기 공속 = 개별 무기 아이템 속성** (무기타입 기본값이 아님!).
- 예: 두손검 대부분 6속이지만 일부 품목은 4·7속. → **카탈로그 `effects.attackSpeed`에 이미 저장됨(398/399)** ✅
- **최종 공속단계** = `무기.attackSpeed − 부스터 단계감소`, clamp[2,9].

**부스터 단계감소** — 스킬 데이터 `x` 필드에 이미 있음 ✅ (`x` = 공속 단계변화)
| 버프 | `x` (단계변화) | 예시 id |
|---|---|---|
| 일반 부스터(소드/엑스/메이스/스피어/폴암/보우/석궁/자벨린/대거/너클/건) | −2 (전 레벨) | 1101004, 3101002 … |
| 매직 부스터(스펠부스터) | Lv1~10 −1, **Lv11+ −2** | 2111005, 2211005 |
| 윈드 부스터(바이퍼·스트라이커) | Lv1~10 −1, **Lv11+ −2** | 5121009, 15111005 |

→ 부스터의 `x`를 읽어 자동 적용. 별도 매핑 불필요.

### 마법사 시전속도 · 윈드부스터

- 마법사 스킬(익스플로전 제외) = 노말/매직부스터 2값. **스펠부스터 Lv11+** 적용 시 매직부스터 값.
- **윈드부스터(파티버프, +2공속)** 는 메랜에서 마법사에도 적용되도록 변경됨. **단**:
  - 마법사에 윈드부스터 단독 적용 시 시전속도 = **미확인**.
  - **스펠부스터 + 윈드부스터 동시** 적용 시 = **데이터 없음**.
  - → 둘 다 **현재 미지원**(추정값 or 데이터 확보 시 구현). 안내 코멘트만.
- 윈드부스터는 파티버프로 **추가 예정**(바이퍼·스트라이커 사용, 물리 직업엔 −2단계 정상 적용).

> 실제 분당횟수 표는 12장 참조.

---

## 9. UI 구성 (초안)

기존 몬스터 선택/스탯을 재사용. 대상이 보스인지에 따라 자동 전환.

- **공격 스킬 선택** (직업별 attackSkillsForJob, Lv 조절)
- **비보스**: 방컷 확률표
  - `1방 12.3% · 2방 41.0% · 3방 33.1% · 4방 …`
  - 보조표기: 평균 처치 타수, 1방컷/2방컷 라인
- **보스**: DPM 카드 (+ 처치 소요시간, 데이터 대기 시 안내)
- 모션 혼합/버프/크리 반영 상태를 아이콘/캡션으로 표기(피격 섹션 방식 재사용)

---

## 10. 데이터 스키마 (신규/확장 예정)

스킬별 계산 메타(모션 규칙·라인 수·딜레이·데미지배율)를 담을 테이블 필요.

```ts
// 예시 (docs 기준, 구현 시 조정)
interface SkillCombatMeta {
  skillId: number
  motion: 'general' | 'swing' | 'stab' | 'fixed4' | 'brandish' | 'unsupported'
  lines?: number          // attackCount/bulletCount 보정 (미지정 시 raw 사용)
  delayMs?: number        // ⛳ 공속표
}
interface JobDamageBuff {
  jobId: JobId
  name: string
  multiplier: number      // ⛳
  condition?: string      // 마스터 조건 등
}
interface WeaponSpeed {
  weaponType: WeaponType
  baseSpeedStep: number   // ⛳
}
```

⛳ 표시는 전부 사용자 제공 raw data 대기.

---

## 11. 확정 요약 / 열린 항목

**확정**
- 비보스=정확히 N방 확률(컨볼루션), 보스=DPM
- 모션은 평균 아닌 **확률 혼합분포**로 반영 (2:3 / 폴암 5:5)
- 특수 모션 규칙(브랜디쉬/드래곤쓰레셔/버스터/로어/파이널어택 제외)
- 크리 = 기대배율 근사
- 콤보+어드밴스드 = 1.9× (히어로)

- 위협(팔라딘) = **파티버프**로 관리, 데미지 +7%(마스터) 등 ✅
- 공속표 12장 ✅ / 무기 공속 = `effects.attackSpeed`(카탈로그 보유) ✅
- 부스터 단계감소 = 스킬 `x` 필드 자동(일반 −2, 윈드/스펠 Lv11+ −2) ✅
- 모션 예외 skill id 확정(브랜디쉬/버스터/드래곤쓰레셔/드래곤로어) ✅, 그 외 일반 2:3
- `attackCount` = 단일대상 라인수(브랜디쉬·버스터 2) ✅

**열린 항목 (⛳ 대기)**
1. **직업별 데미지 증가 버프 배율·중첩** (히어로 콤보 1.9× 외) — 유일한 raw data 대기
2. 위협 파티버프 데이터화 + 신규 효과 "몬스터 받는 데미지 +%" 추가
3. 12.4/12.5 표 일부 열정렬 확인 (도적·해적 5·6열 공란)
4. 윈드부스터 파티버프 추가 + 마법사(윈드/스펠+윈드) **미지원** 처리

---

## 12. 공속표 (분당 공격횟수) — raw 기록

출처: 사용자 제공 표. **열 = 공속 단계**: 2(풀공속)·3(매우빠름)·4(빠름)·5(빠름)·6(보통)·7(느림)·8(느림)·9(매우느림). 값 = 분당 공격횟수.

### 12.0 해석 (프레임 기반 · 모션 공유)

- **30fps 프레임 기반**. 분당횟수 → 딜레이(ms)=60000/apm → 프레임=ms×30/1000.
  - 예) 쓰레셔 풀공속 100회 = 600ms = **정확히 18프레임**, 매우빠름 90회 = **20프레임**. 나머지 셀은 원본 정수프레임을 **반올림한 분당횟수**라 역산 시 소수가 나옴.
  - 프레임 증가폭이 단계 5·8에서 작아지는 3단계 주기 = 프레임 양자화 흔적.
- **대부분 스킬 = 평타 모션 기반** → 동일 곡선 공유. 고유 모션 스킬만 예외라 곡선이 다름(고정딜레이+스윙딜레이 비율 차이).
- 따라서 **단일 공식으로 대체하지 않고**, 데이터를 이렇게 구성:
  ```
  canonical '평타 곡선' = [100, 90, 83, 80, 74, 69, 67, 63]   // 공속 2~9
  스킬 → 'basic'(평타곡선 참조) | 고유곡선(자체 배열) | 'fixed'(공속무관 상수) | 'magic'(노말/부스터 2값)
  ```
  `basic` 참조 스킬: 평타·파워스트라이크·슬래시블래스트·차지블로우·쓰레셔·스트레이프·시브즈·트리플스로우·더블샷·에로우류·럭키세븐·디스오더 등 (12.x ※ 목록).
- **인덱싱**: 최종 공속단계 = 무기 기본공속 − 공속상승버프, clamp[2,9] → 곡선[단계] 조회. (무기 기본공속 = ⛳)

### 12.1 공속 무관 (고정)
| 스킬 | 분당 | 비고 |
|---|---|---|
| (전사) 로어 | ~30 | 무기 공속 무관 |
| (궁수) 폭풍의 시 | 500 | |
| (궁수) 피어싱(풀차징) | ~21 | 공속차 거의 없음 |
| (도적) 쉐도우 웹 | 74 | |
| (도적) 부메랑 스텝 | ~48 | |
| (도적) 닌자스톰 | 42 | |
| (해적) 파이어버너·쿨링이펙트 | 57 | |
| (해적) 스내치 | 45 | |
| (마) 힐 | 100 | |
| (마) 샤이닝레이 | 57 | |
| (마) 제네시스 | ~22 | |
| (마) 엔젤레이 | 74 | |

### 12.2 무기 공속 단계 의존 (전사)
| 스킬 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|---|---|
| 샤우트 | 52 | 49 | 45 | 42 | 40 | 37 | 35 | — |
| 쓰레셔 | 100 | 90 | 83 | 80 | 74 | 69 | 67 | 63 |
| 버스터 | 74 | 69 | 66 | 61 | 59 | 53 | 50 | 48 |
| 브랜디쉬/블래스트 | 95 | 87 | 80 | 74 | 72 | 67 | 63 | — |
| 차지블로우(여차) | 100 | 90 | 83 | 80 | 74 | 69 | 67 | — |

※ 평타·파워스트라이크·슬래시블래스트 = 차지블로우/쓰레셔와 동일. 사수(석궁)·건슬(건) 제외 전 직업 평타 동일.

### 12.3 무기 공속 단계 의존 (궁수)
| 스킬 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|
| 스트레이프(활) | 100 | 90 | 83 | 80 | 74 |
| 스트레이프(석궁) | 94 | 86 | 82 | 76 | 70 |

※ 평타·더블샷·에로우블로우·에로우봄·아이언에로우·레인·이럽션·파이어샷·아이스샷·드래곤펄스 = 스트레이프 동일.

### 12.4 무기 공속 단계 의존 (도적)  ⚠️일부 열정렬 확인필요
| 스킬 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|
| 세비지블로우 | 83 | 77 | 72 | 67 | — |
| 시브즈 | 100 | 90 | 83 | 80 | — |
| 어벤저 | 95 | 87 | 80 | — | 72 |
| 트리플스로우 | 100 | 90 | 83 | — | 74 |
| 쇼다운 | 58 | 54 | 50 | — | 44 |

※ 평타·더블스텝·럭키세븐·디스오더·스틸·드레인·쉐도우메소 = 트스/시브즈 동일. (어벤저·트스·쇼다운의 5열 공란=6열 값인지 확인 필요)

### 12.5 무기 공속 단계 의존 (해적)  ⚠️일부 열정렬 확인필요
| 스킬 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|
| 스트레이트 | 130 | 114 | 108 | 104 | 101 |
| 써머솔트킥 | 89 | 84 | 77 | 72 | 69 |
| 더블어퍼 | 53 | 53 | 53 | 53 | 51 |
| 인비저블샷 | 95 | 87 | — | 77 | — |
| 쇼크웨이브 | 40 | 37 | 34 | 32 | 31 |
| 트리플파이어 | 91 | 87 | — | 74 | — |
| 데몰리션 | 26 | 24 | 22 | 21 | 20 |
| 배틀쉽 캐논 | 100 | 96 | — | 80 | — |
| 배틀쉽 토르페도 | 83 | 77 | — | 74 | — |

※ 평타 = 인비저블샷 동일, 페이크샷 = 트리플파이어 동일.

### 12.6 매직부스터 의존 (마법사, 무기 공속 무관)
| 스킬 | 노말 | 매직부스터 |
|---|---|---|
| 아이스스트라이크 | 57 | 64 |
| 썬더스피어 | 45 | 53 |
| 매직컴포지션 | 67 | 74 |
| 포이즌미스트 | 40 | 45 |
| 페럴라이즈 | 74 | 83 |
| 체인라이트닝 | 77 | 87 |
| 빅뱅(노차징 난사) | ~62 | ~73 |
| 메테오/블리자드 | ~17 | ~19 |

※ 매직클로·에너지볼트·파이어에로우·포이즌브레스·콜드빔·썬더볼트·홀리에로우·파이어/아이스데몬 = 페럴라이즈 동일. 익스플로전 제외.
